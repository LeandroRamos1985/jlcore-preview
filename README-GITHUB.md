# GitHub Pages update — V2.6

Upload **all files from this folder** into the root of the existing `jlcore-preview` repository. Do not upload the outer folder itself.

GitHub Pages remains:
`https://leandroramos1985.github.io/jlcore-preview/`

After committing, wait for the Pages deployment and hard-refresh the page with Ctrl+F5.

This preview is intentionally `noindex,nofollow` and should not replace the production website until client approval, high-resolution media, forms/CRM and IDX/MLS decisions are complete.
